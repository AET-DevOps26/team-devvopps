apiVersion: v1
kind: Service
metadata:
  name: client
  namespace: {{ .Values.namespace }}
spec:
  type: NodePort
  selector:
    app: client
  ports:
    - port: {{ .Values.services.client.port }}
      targetPort: {{ .Values.services.client.port }}
      nodePort: 30000
