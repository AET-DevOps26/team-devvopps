apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: {{ .Values.namespace }}
spec:
  type: ClusterIP
  selector:
    app: user-service
  ports:
    - port: {{ .Values.services.userService.port }}
      targetPort: {{ .Values.services.userService.port }}
