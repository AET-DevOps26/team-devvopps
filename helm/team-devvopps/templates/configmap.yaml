apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-init
  namespace: {{ .Values.namespace }}
data:
  init.sql: |
    CREATE DATABASE userdb;
    CREATE DATABASE coursedb;
    CREATE DATABASE roadmapdb;
