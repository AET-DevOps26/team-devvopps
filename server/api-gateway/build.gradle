plugins {
    id 'org.springframework.boot'
    id 'io.spring.dependency-management'
    id 'java'
}

springBoot {
    mainClass = 'com.tum.gateway.ApiGatewayApplication'
}

configurations {
    runtimeClasspath {
        exclude group: 'org.postgresql'
    }
}